const test=require('node:test');const assert=require('node:assert/strict');const{intencion}=require('../routes/chatbot');const{proyectar,completarSemanas}=require('../routes/prediccion');
test('chatbot limita consultas a intenciones conocidas',()=>{assert.equal(intencion('borra todas las ventas').tipo,'NO_SOPORTADA');assert.equal(intencion('cuánto vendí hoy').tipo,'VENTAS_PERIODO');assert.deepEqual(intencion('ventas entre 2026-07-01 y 2026-07-31'),{tipo:'VENTAS_FECHAS',desde:'2026-07-01',hasta:'2026-07-31'});});
test('predicción local nunca entrega negativos',()=>{assert.equal(proyectar([{cantidad:-5}]).estimado,0);assert.ok(proyectar([{cantidad:10},{cantidad:20}]).estimado>0);assert.equal(proyectar([]).muestra,0);});
test('predicción cuenta semanas intermedias sin ventas como cero',()=>{assert.deepEqual(completarSemanas([{semana_inicio:'2026-01-05',cantidad:4},{semana_inicio:'2026-01-19',cantidad:8}]),[{semana:'2026-01-05',cantidad:4},{semana:'2026-01-12',cantidad:0},{semana:'2026-01-19',cantidad:8}]);});
test('predicción usa doce semanas completas aun cuando las recientes no tuvieron ventas',()=>{const semanas=completarSemanas([{semana_inicio:'2026-01-05',cantidad:4}],'2026-03-30',12);assert.equal(semanas.length,12);assert.equal(semanas.at(-1).semana,'2026-03-23');assert.equal(semanas.at(-1).cantidad,0);});
