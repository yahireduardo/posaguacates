const test=require('node:test');const assert=require('node:assert/strict');const bcrypt=require('bcryptjs');const{resolverAutorizacionAdmin}=require('../lib/autorizacionAdmin');
test('ADMON_GRAL debe volver a escribir su contraseña',async()=>{const hash=await bcrypt.hash('Segura12345',4);const r=await resolverAutorizacionAdmin({usuario:{id:1,username:'admin',rol:'ADMON_GRAL'},body:{password_admin:'Segura12345'},buscarAdministrador:async()=>({id:1,password_hash:hash})});assert.deepEqual(r,{solicitadoPor:1,autorizadoPor:1,delegada:false});});
test('ADMON_GRAL es rechazado sin reautenticación',async()=>{const hash=await bcrypt.hash('Segura12345',4);await assert.rejects(resolverAutorizacionAdmin({usuario:{id:1,username:'admin',rol:'ADMON_GRAL'},body:{},buscarAdministrador:async()=>({id:1,password_hash:hash})}),e=>e.status===401);});
test('CAJERO requiere credenciales administrativas',async()=>{await assert.rejects(resolverAutorizacionAdmin({usuario:{id:2,rol:'CAJERO'},body:{},buscarAdministrador:async()=>null}),e=>e.status===400);});
test('CAJERO acepta autorización bcrypt correcta y conserva su identidad',async()=>{const hash=await bcrypt.hash('Segura12345',4);const r=await resolverAutorizacionAdmin({usuario:{id:2,rol:'CAJERO'},body:{usuario_admin:'admin',password_admin:'Segura12345'},buscarAdministrador:async()=>({id:1,password_hash:hash})});assert.deepEqual(r,{solicitadoPor:2,autorizadoPor:1,delegada:true});});
test('CAJERO recibe mensaje genérico con autorización incorrecta',async()=>{const hash=await bcrypt.hash('Segura12345',4);await assert.rejects(resolverAutorizacionAdmin({usuario:{id:2,rol:'CAJERO'},body:{usuario_admin:'admin',password_admin:'incorrecta'},buscarAdministrador:async()=>({id:1,password_hash:hash})}),e=>e.status===401);});
test('otros roles son rechazados',async()=>{await assert.rejects(resolverAutorizacionAdmin({usuario:{id:3,rol:'ADMIN'},body:{},buscarAdministrador:async()=>null}),e=>e.status===403);});
