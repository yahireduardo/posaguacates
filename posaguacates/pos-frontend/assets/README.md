# Logo del ticket

Coloca en esta carpeta el logo con el nombre exacto:

`logo-ticket.png`

La ruta pública será `/assets/logo-ticket.png`. Si el archivo no existe o no
puede cargarse, el ticket oculta automáticamente el espacio reservado.
